# Create a local .env file from this example only if you use a dotenv loader.
# For now, set this as a Windows environment variable instead. Never commit a real key.
GEMINI_API_KEY=replace_with_your_key
